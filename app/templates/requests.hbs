{{page-title "Requests"}}
<section class="au-o-layout au-o-region-large">
  <div class="au-o-grid au-o-grid--small">
    <div class="au-o-grid__item">
      <AuContent @skin="large">
        <h1>Application dashboard</h1>

        <ProtectedMonumentSelect @selectedMonument={{this.selectedMonument}} />
        <LawDocumentSelect @selectedMonument={{this.selectedLaw}} />
      </AuContent>
    </div>
  </div>
</section>