{{#if this.showEnvironment}}
  {{page-title (concat this.environmentName)}}
{{/if}}
<AuApp>

  <Header />
  {{#if this.session.isAuthenticated}}{{/if}}
  <AuMainContainer class="au-u-flex" as |m|>
    <m.content @scroll={{true}}>
      <AuBodyContainer @scroll={{true}}>
        {{outlet}}
      </AuBodyContainer>
    </m.content>
  </AuMainContainer>
</AuApp>
<AuModalContainer />
<AuToaster />