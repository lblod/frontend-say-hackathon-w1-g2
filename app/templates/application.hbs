{{page-title "Hackathon Group 2"}}

<AuModalContainer />

<AuApp class="{{if this.showEnvironment 'au-c-app--environment'}}">

  <Header />

  <AuMainContainer class="au-u-flex" as |m|>
    <m.content @scroll={{true}}>
      <AuBodyContainer @scroll={{true}}>
        {{outlet}}
      </AuBodyContainer>
    </m.content>
  </AuMainContainer>
</AuApp>